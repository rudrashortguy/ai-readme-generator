services:
  backend:
    build: ./backend
    ports: ["8001:8001"]
    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
